{
  "name": "api-gateway-reseller",
  "version": "0.1.0",
  "private": true,
  "description": "API forwarding gateway with API key management, wallet billing, and usage analytics.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/api & npm run dev --workspace apps/web",
    "dev:api": "npm run dev --workspace apps/api",
    "dev:web": "npm run dev --workspace apps/web",
    "build": "bash scripts/release-build-guard.sh && /usr/local/bin/api-gateway-resource-guard.sh --preflight-build && npm run db:generate && npm run build --workspace packages/db && npm run build --workspace apps/api && npm run build --workspace apps/web",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run db:generate && npm run build --workspace packages/db && npm run typecheck --workspace apps/api && npm run typecheck --workspace apps/web",
    "audit:codex-upstreams": "dotenv -e .env -- tsx scripts/audit-upstream-codex-roundtrip.ts",
    "audit:upstream-compression": "dotenv -e .env -- tsx scripts/audit-upstream-compression.ts",
    "audit:policy-recovery-compact-live": "dotenv -e .env -- tsx scripts/audit-policy-recovery-compact-live.ts",
    "audit:policy-recovery-live": "dotenv -e .env -- tsx scripts/audit-policy-recovery-live.ts",
    "audit:policy-recovery-parity": "dotenv -e .env -- tsx scripts/audit-policy-recovery-exe-parity.ts",
    "load:saturation": "node scripts/saturation-load-test.mjs",
    "db:generate": "bash scripts/release-build-guard.sh && npm run db:generate --workspace packages/db",
    "db:migrate": "npm run db:migrate --workspace packages/db",
    "db:migrate:deploy": "npm run db:migrate:deploy --workspace packages/db",
    "db:seed": "npm run db:seed --workspace packages/db",
    "db:studio": "npm run db:studio --workspace packages/db",
    "audit:policy-recovery-profiles": "dotenv -e .env -- tsx scripts/audit-policy-recovery-profiles.ts",
    "sync:security-research-skill": "node scripts/sync-security-research-skill.mjs",
    "verify:security-research-skill": "node scripts/verify-security-research-skill.mjs",
    "verify:security-research-skill:remote": "node scripts/verify-security-research-skill.mjs --remote"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.24",
    "prettier": "^3.7.4",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
